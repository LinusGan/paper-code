下面这版是在你原始研究设定基础上，合并你刚确认的修改后重写的研究笔记版本。原始主线包括：以 CSI300 为对象、使用 2018–2024 年时间区间、以 FinBERT 处理新闻文本、以 Transformer 为主模型，并比较 concat 与 gate 两类融合方式。

# 基于市场数据与新闻文本融合的沪深300指数短期预测研究笔记

---

# 一、研究定位

## 1.1 研究背景

金融市场价格不仅受到历史行情与技术指标影响，也会受到新闻文本、市场情绪与外部事件冲击的驱动。传统基于数值时间序列的预测方法通常只能利用价格与成交量等结构化信息，难以有效吸收新闻文本中隐含的情绪与语义信号。因此，将市场数值数据与新闻文本信息进行融合，构建日频多模态预测框架，是一个具有现实意义与研究价值的方向。

在本研究中，我将以沪深300指数（CSI300）为预测对象，构建一个融合市场数据与新闻文本信息的短期预测框架，重点研究文本信息是否具有增量预测能力，以及不同融合机制在金融日频任务中的适用性差异。

---

## 1.2 研究目标

本研究的总体目标是：

1. 构建一个适用于金融日频预测任务的多模态框架；
2. 系统评估新闻文本信息对指数短期预测的边际贡献；
3. 设计并实现一种适用于金融日频多模态预测的门控融合框架；
4. 比较门控融合与简单拼接融合在该任务中的表现差异；
5. 分析文本信息在不同市场状态下的有效性；
6. 从机器学习指标与经济解释两个维度评估模型有效性。

---

## 1.3 研究问题

本研究拟重点回答以下问题：

### 研究问题1
仅使用市场数值特征，是否能够有效预测沪深300指数下一交易日走势？

### 研究问题2
在数值特征基础上加入新闻文本信息后，模型预测能力是否显著提升？

### 研究问题3
相比于简单拼接（Concat）方式，门控融合（Gated Fusion）是否能够更有效地整合数值与文本信息？

### 研究问题4
不同文本表示方式（情绪概率聚合 vs CLS embedding 聚合）对预测结果有何影响？

### 研究问题5
文本信息的预测贡献是否具有市场状态依赖性，例如在高波动阶段、下跌阶段或重大冲击阶段更有效？

### 研究问题6
模型提升是否不仅体现在分类指标上，也能够转化为一定的经济意义，例如策略收益、Sharpe比率或回撤控制表现更优？

---

# 二、研究思路概述

## 2.1 总体框架

本研究采用“数值分支 + 文本分支 + 融合模块 + 预测层”的整体设计。

整体流程如下：

```text
市场数值数据 ──> 序列模型（Transformer / LSTM） ──> h_num
新闻文本数据 ──> FinBERT表示学习 / 聚合处理 ──> h_text
                                            ↓
                                  融合模块（Concat / Gate）
                                            ↓
                                       预测输出层
                                            ↓
                           分类结果 / 回归结果 / 策略信号
```

------

## 2.2 研究主线

本研究将以“下一交易日方向预测”为主任务，同时扩展“下一交易日收益率预测”作为辅助或并行任务：

- **方案B**：主任务分类 + 辅助任务回归
- **方案C**：将分类预测结果映射到简单交易策略，评估经济意义

最终可根据实验结果决定以哪一条主线作为论文核心结果呈现方式，但两个方案都会进行实现和比较。

------

# 三、研究对象与样本设计

## 3.1 预测对象

- 预测标的：沪深300指数（CSI300）
- 频率：日频
- 预测目标：下一交易日

选择沪深300的原因：

1. 代表A股核心大盘蓝筹市场，研究意义较强；
2. 数据可得性较高，结构化行情数据稳定；
3. 与宏观、政策、舆情等新闻因素相关性较强；
4. 比个股更适合作为本科论文中的指数级预测对象，噪声相对更可控。

------

## 3.2 时间区间

建议样本区间为：

**2018-01-01 至 2024-12-31**

该区间的优点包括：

1. 覆盖较完整的市场周期；
2. 包含疫情冲击、经济修复、政策波动、风险偏好变化等多种市场状态；
3. 文本数据在近几年通常更完整、质量更稳定；
4. 样本量适中，既不至于过短，也不会在本科阶段造成过重的数据工程负担。

------

## 3.3 数据划分

采用严格的时间序列划分方式：

| 数据集 | 时间区间                |
| ------ | ----------------------- |
| Train  | 2018-01-01 ~ 2022-12-31 |
| Valid  | 2023-01-01 ~ 2023-12-31 |
| Test   | 2024-01-01 ~ 2024-12-31 |

说明：

- 不进行随机打乱；
- 保证训练、验证、测试严格按时间顺序展开；
- 避免未来信息泄露；
- 若后续条件允许，可再增加滚动窗口回测作为补充验证方式。

------

# 四、预测任务设计

## 4.1 分类任务

基础分类标签定义为下一交易日涨跌方向：

[
y^{cls}*{t+1} =
\begin{cases}
1, & r*{t+1} > 0 \
0, & r_{t+1} \le 0
\end{cases}
]

其中：

- ( r_{t+1} ) 表示下一交易日收益率；
- 该任务用于检验模型对方向的识别能力。

------

## 4.2 回归任务

辅助任务定义为下一交易日收益率预测：

[
y^{reg}*{t+1} = r*{t+1}
]

该任务的意义在于：

1. 让模型不仅学习涨跌方向，还学习收益幅度；
2. 为后续的经济解释提供更丰富依据；
3. 用于分析文本信息是否不仅影响方向判断，也影响收益波动的刻画能力。

------

## 4.3 经济解释任务

在分类或回归输出基础上，将模型预测结果映射为简单策略信号：

- 若预测上涨，则做多指数；
- 若预测下跌，则空仓或反向信号处理；
- 比较模型策略与基准策略的收益表现。

评价内容可包括：

- 累计收益率
- 年化收益率
- Sharpe Ratio
- 最大回撤
- 胜率

这一部分不作为高频交易研究，而是作为“模型是否具有经济意义”的补充验证。

------

# 五、数据设计

## 5.1 数据结构总览

本研究以“交易日”为主键构建日频主表，每个交易日对应一行样本，包含：

1. 市场数值特征；
2. 文本特征；
3. 标签信息；
4. 数据集划分标记。

------

## 5.2 数值数据字段设计

### 5.2.1 原始行情字段

- `open`
- `high`
- `low`
- `close`
- `volume`

------

### 5.2.2 收益率特征

- `ret_1`：1日收益率
- `ret_2`：2日收益率
- `ret_3`：3日收益率
- `ret_5`：5日收益率
- `ret_10`：10日收益率
- `ret_20`：20日收益率

设计原因：

- 捕捉短中期价格变动趋势；
- 为模型提供动量与反转的基础信息。

------

### 5.2.3 波动率与振幅特征

- `volatility_5`
- `volatility_10`
- `volatility_20`
- `hl_range`：高低价振幅
- `oc_return`：开收盘收益
- `gap_return`：跳空收益

设计原因：

- 波动率与振幅有助于刻画风险状态；
- 对“新闻在高波动日是否更有效”的研究有支持作用。

------

### 5.2.4 趋势与技术指标特征

- `ma_5`
- `ma_10`
- `ma_20`
- `ma_gap_5`
- `ma_gap_10`
- `ma_gap_20`
- `rsi_14`
- `momentum_5`
- `momentum_10`

设计原因：

- 描述市场趋势、超买超卖状态与价格偏离程度；
- 为数值分支提供较完整的日频技术形态信息。

------

### 5.2.5 量价关系特征

- `volume_change_1`
- `volume_change_5`
- `price_volume_corr_5`
- `price_volume_corr_10`

设计原因：

- 量能变化通常反映市场参与度；
- 新闻冲击若伴随成交量变化，可能更具解释意义。

------

### 5.2.6 市场状态辅助特征

- `trend_regime`
- `vol_regime`
- `drawdown_state`

说明：

这些特征可以作为输入，也可以只用于后续分组分析。其目的在于为异质性检验提供市场状态划分依据。

------

## 5.3 文本数据字段设计

文本数据以新闻为基础，经过日频聚合后形成交易日级特征。

### 5.3.1 原始新闻字段

每条新闻至少包含：

- `news_id`
- `publish_time`
- `publish_date`
- `title`
- `content`

------

### 5.3.2 新闻统计字段

对每个交易日统计：

- `news_count`

必要时可增加：

- `title_count`
- `content_len_mean`

------

## 5.4 两层文本表示设计

本研究采用两层文本表示方案。

------

### 第一层：基础文本表示（情绪概率聚合）

对每条新闻使用 FinBERT 输出三类情绪概率：

- `pos`
- `neg`
- `neu`

然后按交易日聚合，形成以下字段：

- `finbert_pos_mean`
- `finbert_neg_mean`
- `finbert_neu_mean`

进一步构造衍生情绪特征：

- `sentiment_score = pos_mean - neg_mean`
- `sentiment_abs = |sentiment_score|`
- `sentiment_dispersion`（可选）
- `neg_ratio`（可选）

这一层的特点是：

- 维度低；
- 可解释性强；
- 适合作为主实验的稳定文本输入。

------

### 第二层：增强文本表示（CLS Embedding 聚合）

对每条新闻提取 FinBERT 的 CLS 向量表示，然后按日进行聚合，例如：

- 均值池化（mean pooling）
- 加权池化（可按发布时间或新闻长度加权）
- Top-k 重要新闻池化（扩展可选）

得到交易日级文本向量：

- `cls_emb_1`
- `cls_emb_2`
- ...
- `cls_emb_d`

这一层的特点是：

- 能保留比情绪概率更丰富的语义信息；
- 能够验证“文本只保留情绪概率是否损失了有效信息”；
- 作为增强实验可以提升论文层次。

------

## 5.5 无新闻日处理规则

对于无新闻交易日，基础规则设定为：

- `news_count = 0`
- `finbert_pos_mean = 0`
- `finbert_neg_mean = 0`
- `finbert_neu_mean = 1`
- `sentiment_score = 0`
- `sentiment_abs = 0`

对于 embedding 方案，可采用：

- 全零向量；
- 或沿用前一交易日向量；
- 或增加 `has_news` 指示变量。

最终采用哪一种处理方式，可在验证集中比较后确定。

------

## 5.6 时间对齐规则

这是本研究中非常关键的一部分。

### 对齐原则

仅允许使用在预测时点之前能够观察到的信息。

### 具体规则

1. 使用 **t日收盘前发布** 的新闻来预测 **t+1日**；
2. 若新闻发布时间晚于 t 日收盘，则归入下一交易日；
3. 非交易日发布的新闻，映射到下一个交易日；
4. 所有聚合操作必须严格基于可得时间信息完成。

这部分将作为论文中“防止信息泄露”的核心说明。

------

# 六、主表结构设计

最终主表可设计为如下结构：

```text
trade_date,
open, high, low, close, volume,
ret_1, ret_2, ret_3, ret_5, ret_10, ret_20,
volatility_5, volatility_10, volatility_20,
hl_range, oc_return, gap_return,
ma_5, ma_10, ma_20,
ma_gap_5, ma_gap_10, ma_gap_20,
rsi_14, momentum_5, momentum_10,
volume_change_1, volume_change_5,
price_volume_corr_5, price_volume_corr_10,
trend_regime, vol_regime, drawdown_state,
news_count,
finbert_pos_mean, finbert_neg_mean, finbert_neu_mean,
sentiment_score, sentiment_abs,
has_news,
next_ret_1, y_cls,
split
```

如果使用 embedding 版本，则额外维护单独文本向量矩阵，不建议直接展开在宽表中。

------

# 七、模型设计

## 7.1 总体模型分层

本研究将模型分为三类：

1. 传统机器学习基线；
2. 序列深度学习基线；
3. 多模态融合主模型。

------

## 7.2 基线模型

### 7.2.1 XGBoost

作用：

- 作为强结构化特征基线；
- 检验复杂时序模型是否真正优于树模型；
- 适合处理手工构造后的数值与情绪聚合特征。

输入：

- 当日或过去窗口聚合后的数值与文本特征。

------

### 7.2.2 LSTM

作用：

- 作为经典时间序列深度学习基线；
- 与 Transformer 对比，验证是否确实需要注意力机制。

输入：

- 长度为 (L) 的历史窗口序列。

输出：

- 时间序列隐藏状态表示 (h_{num})。

------

## 7.3 主模型：Transformer 多模态框架

### 数值分支

使用 time-series Transformer 编码过去 (L) 个交易日的数值特征序列：

[
X_{num} \in \mathbb{R}^{N \times L \times D_{num}}
]

输出数值表示：

[
h_{num} \in \mathbb{R}^{d}
]

------

### 文本分支

文本分支设计两种方案：

#### 方案A：情绪概率序列输入

将过去 (L) 个交易日的日级情绪特征序列输入线性层或轻量编码器，得到：

[
h_{text} \in \mathbb{R}^{d}
]

#### 方案B：CLS embedding 序列输入

将过去 (L) 个交易日的文本 embedding 序列输入文本编码层或投影层，得到：

[
h_{text} \in \mathbb{R}^{d}
]

------

## 7.4 融合机制设计

### 方法1：Concat 融合

最直接的融合方式是向量拼接：

[
h = [h_{num}; h_{text}]
]

然后通过 MLP 输出预测结果。

特点：

- 实现简单；
- 作为标准对照组；
- 可用于检验简单融合是否已经足够。

------

### 方法2：Gated Fusion 融合

门控融合为本研究的核心方法设计之一，其目标是动态控制数值信息与文本信息在不同样本中的贡献比例。

定义为：

[
g = \sigma(W[h_{num}; h_{text}] + b)
]

[
h = g \odot h_{num} + (1-g) \odot h_{text}
]

其中：

- (g) 为门控权重；
- (\sigma) 为 sigmoid 函数；
- (\odot) 表示逐元素乘法。

该机制的直观含义是：

- 当文本信息更有价值时，模型可提高文本分支权重；
- 当市场数值特征更可靠时，模型可偏向数值分支；
- 该机制更符合金融市场中“不同状态下不同信息源重要性不同”的实际逻辑。

本研究对该方法的表述不强调算法首创，而强调其在金融日频多模态预测任务中的适配与验证。

------

# 八、模型输入设计

## 8.1 时间窗口

设定历史窗口长度为：

- `lookback = 20`

原因：

1. 20个交易日约等于1个月；
2. 能覆盖较短期趋势与新闻累积效应；
3. 复杂度适中，便于训练与调参。

后续可扩展比较：

- 10日
- 20日
- 30日

------

## 8.2 输入张量结构

### 数值输入

[
X_{num} \in \mathbb{R}^{N \times L \times D_{num}}
]

### 文本输入

基础版：
[
X_{text}^{sent} \in \mathbb{R}^{N \times L \times D_{sent}}
]

增强版：
[
X_{text}^{emb} \in \mathbb{R}^{N \times L \times D_{emb}}
]

### 分类标签

[
y^{cls} \in \mathbb{R}^{N}
]

### 回归标签

[
y^{reg} \in \mathbb{R}^{N}
]

------

# 九、实验设计

## 9.1 实验层次

本研究的实验设计分为四层：

1. 文本是否有效；
2. 融合机制是否有效；
3. 模型结构是否有效；
4. 文本表示方式是否有效。

------

## 9.2 对照模型设置

### 第一组：单模态基线

- Num-only（仅数值）
- Text-only（仅文本）

### 第二组：融合方式对比

- Concat
- Gate

### 第三组：模型结构对比

- XGBoost
- LSTM
- Transformer

### 第四组：文本表示对比

- 情绪概率聚合
- CLS embedding 聚合

------

## 9.3 重点实验

### 实验1：文本增量价值检验

比较：

- Num-only
- Num + Text(Concat)
- Num + Text(Gate)

目标：

检验加入文本后模型性能是否提升。

------

### 实验2：融合机制对比

比较：

- Concat
- Gate

目标：

检验门控融合是否优于简单拼接。

------

### 实验3：模型结构对比

比较：

- XGBoost
- LSTM
- Transformer

目标：

检验复杂模型是否真正带来额外收益。

------

### 实验4：文本表示对比

比较：

- 情绪概率聚合
- CLS embedding 聚合

目标：

检验 richer semantic representation 是否优于简单情绪统计。

------

### 实验5：分类 vs 回归

比较：

- 纯分类任务
- 分类 + 回归多任务
- 回归结果转方向标签

目标：

检验收益预测是否能增强方向预测。

------

### 实验6：经济解释检验

将不同模型输出转化为简单策略信号，比较：

- 累计收益
- Sharpe Ratio
- 最大回撤
- 胜率

目标：

评估模型改进是否具备经济含义。

------

### 实验7：市场状态异质性分析

按以下维度进行分组：

- 高波动 vs 低波动
- 上涨阶段 vs 下跌阶段
- 冲击期 vs 平稳期

目标：

分析文本信息在哪些市场状态下更有用。

------

# 十、评价指标设计

## 10.1 分类指标

- Accuracy
- Precision
- Recall
- F1-score
- AUC
- Balanced Accuracy
- MCC（可选）

说明：

仅使用 Accuracy 容易受类别分布影响，因此需要加入更稳健的分类指标。

------

## 10.2 回归指标

- MSE
- RMSE
- MAE
- IC（可选）
- Rank IC（可选）

------

## 10.3 经济指标

- Cumulative Return
- Annualized Return
- Sharpe Ratio
- Max Drawdown
- Win Rate

------

# 十一、预处理与训练细节

## 11.1 标准化

### 数值特征

采用训练集统计量进行 Z-score 标准化：

[
x' = \frac{x - \mu_{train}}{\sigma_{train}}
]

注意：

- 验证集与测试集必须使用训练集均值和标准差；
- 不允许用全样本统计量。

### 文本特征

- 概率特征一般可不标准化；
- embedding 特征可考虑标准化或投影层处理。

------

## 11.2 样本不平衡处理

若涨跌样本分布不均衡，可考虑：

- class weight
- focal loss
- balanced sampling

但应避免破坏时间顺序。

------

## 11.3 训练控制

需记录并固定：

- random seed
- batch size
- learning rate
- hidden size
- dropout
- early stopping

必要时进行多次重复实验，报告平均值和标准差。

------

## 11.4 信息泄露防控

必须重点控制以下问题：

1. 新闻发布时间是否晚于交易收盘；
2. 特征构造是否使用未来窗口；
3. 标准化是否误用全样本；
4. 滚动窗口构造是否跨越测试边界；
5. 标签是否与输入期重叠。

这部分是论文可信度的关键环节。

------

# 十二、研究贡献表述

本研究预期贡献包括：

1. 构建一个融合市场数值特征与新闻文本信息的金融日频多模态预测框架；
2. 设计并实现一种适用于金融日频多模态预测的门控融合框架，并验证其相对简单拼接融合的适用性；
3. 系统比较不同文本表示方式（情绪概率聚合与 CLS embedding 聚合）在指数预测任务中的表现差异；
4. 从分类性能与经济意义两个维度检验文本信息的增量价值；
5. 分析文本信息在不同市场状态下的有效性，为金融文本信号的应用边界提供实证依据。

------

# 十三、可能的风险与应对

## 风险1：文本数据质量不稳定

应对：

- 先保证标题级文本可用；
- 缺失内容时优先保留标题；
- 做新闻数量与覆盖率统计。

## 风险2：模型提升不显著

应对：

- 加强特征工程；
- 优化时间对齐；
- 分市场状态分析；
- 比较不同文本表示；
- 不强求所有阶段都显著提升，重点分析有效区间。

## 风险3：模型过于复杂、实现成本高

应对：

- 先完成情绪概率版本；
- 再扩展 CLS embedding 版本；
- 优先保证主实验闭环完成。

## 风险4：论文像工程项目而不像研究

应对：

- 强化研究问题表述；
- 强化异质性分析；
- 强化经济解释；
- 强化基线对照与误差分析。

------

# 十四、当前执行路线

## 第一阶段：数据准备

1. 获取 CSI300 日频行情数据；
2. 获取新闻数据并清洗；
3. 完成新闻与交易日对齐；
4. 构建主表。

## 第二阶段：文本处理

1. 用 FinBERT 输出情绪概率；
2. 按日聚合生成基础文本特征；
3. 提取 CLS embedding 并聚合生成增强文本特征。

## 第三阶段：建模实现

1. 搭建 XGBoost 基线；
2. 搭建 LSTM 基线；
3. 搭建 Transformer 主模型；
4. 实现 Concat 与 Gate 两类融合。

## 第四阶段：实验评估

1. 完成分类实验；
2. 完成回归实验；
3. 完成经济解释实验；
4. 完成市场状态异质性分析。

## 第五阶段：论文写作

1. 写研究背景与问题定义；
2. 写数据与方法；
3. 写实验结果；
4. 写讨论与结论；
5. 完善创新点与局限性表述。

------

